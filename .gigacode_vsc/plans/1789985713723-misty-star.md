# Plan: Simplify CSV → dates, prices, lags

## Goal
Make it dead simple: user provides a CSV, code extracts **date** and **price** into separate lists, then computes **lag lists** into separate lists.

## Changes to `main.py`

### 1. Simplify `load_coffee` → `load_csv`
- Accept CSV path
- Read CSV with pandas
- Extract `date` column → list of datetime objects
- Extract `price` column → list of floats (using `parse_number`)
- Return `(dates, prices)` — two plain Python lists

### 2. Simplify `make_lags`
- Accept `prices` list and `lags` tuple
- Return `(lag_list, price_list)` — two separate lists
- `lag_list`: each row = `[prices[i-lag1], prices[i-lag2], ...]`
- `price_list`: each element = `prices[i]`
- Keep as-is (it's already clean)

### 3. Update `train_model`
- Call `load_csv('data.csv')` → `(dates, prices)`
- Call `make_lags(prices, lags)` → `(lag_list, price_list)`
- Convert to numpy arrays as before

### 4. Update `get_lags`
- Call `load_csv('data.csv')` → `(dates, prices)`
- Extract last `max(lags)` prices
- Return lag values

### 5. Keep everything else unchanged
- `parse_number`, `SimpleMLP`, `train_model`, `predict`, CLI — all stay the same

## Result
```python
# User flow:
dates, prices = load_csv('my_data.csv')   # two lists
lag_list, price_list = make_lags(prices)  # two lists for training
```

No plan file changes needed — just editing `main.py`.
